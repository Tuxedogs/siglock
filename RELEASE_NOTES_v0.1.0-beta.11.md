# SigLock v0.1.0 Beta 11 — Open Beta

## Fixed

- Removed the opaque startup splash from the full-screen OCR region picker.
- The desktop is now visible immediately while dragging a capture area for OCR.

## Validation

- Type checks, feature acceptance checks, production build, and Rust tests are run before release publication.
- GitHub Actions produces the Windows installer and signed updater artifacts after the `v0.1.0-beta.11` tag is published.
