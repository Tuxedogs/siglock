# SigLock v0.1.0 Beta 8 — Open Beta

## Fixed

- Set the sidebar utility/build footer text to a consistent 9px size.
- Fixed expanded Settings categories clipping into adjacent content by allowing Settings to scroll cleanly.
- Removed the duplicate OCR capture preview from Settings.

## Validation

- Type checks, feature acceptance checks, production build, and Rust tests are run before release publication.
- GitHub Actions produces the Windows installer and signed updater artifacts after the `v0.1.0-beta.8` tag is published.
